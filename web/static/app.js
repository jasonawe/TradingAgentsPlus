(() => {
  "use strict";

  const I18N = {
    en: {
      "brand.localDesk": "LOCAL RESEARCH DESK",
      "connection.ready": "Ready", "connection.live": "Live", "connection.reconnecting": "Reconnecting",
      "connection.complete": "Complete", "connection.failed": "Failed", "connection.cancelled": "Cancelled",
      "setup.kicker": "MULTI-AGENT MARKET INTELLIGENCE", "setup.titleLine1": "Frame the question.", "setup.titleLine2": "Let the desk investigate.",
      "setup.lede": "Configure a focused research run, watch each specialist work, and receive a decision-ready briefing in one place.",
      "setup.credentials": "Credentials stay in your local environment. This desk only exposes non-sensitive model metadata.", "setup.loadingConfig": "Loading desk configuration...", "setup.configUnavailable": "Local configuration unavailable",
      "form.ticker": "Ticker", "form.tickerPlaceholder": "AAPL or BTC-USD", "form.assetType": "Asset type", "form.analysisDate": "Analysis date", "form.analystDesk": "Analyst desk", "form.researchDepth": "Research depth",
      "assets.stock": "Stock", "assets.crypto": "Crypto", "analysts.market": "Market Analyst", "analysts.social": "Sentiment Analyst", "analysts.news": "News Analyst", "analysts.fundamentals": "Fundamentals Analyst", "depth.quick": "Quick", "depth.balanced": "Balanced", "depth.deep": "Deep",
      "actions.startAnalysis": "Start analysis", "actions.refresh": "Refresh", "actions.backHistory": "Back to history", "actions.cancelRun": "Cancel run", "actions.downloadMarkdown": "Download markdown", "actions.newAnalysis": "New analysis", "actions.open": "Open",
      "history.kicker": "ARCHIVE", "history.title": "Previous briefings", "history.loading": "Loading history...", "history.empty": "No briefings saved yet.", "history.unavailable": "History unavailable.", "history.unknown": "Unknown",
      "run.liveBriefing": "LIVE BRIEFING", "run.inProgress": "Analysis in progress", "run.briefingTitle": "{ticker} briefing", "run.deskProgress": "DESK PROGRESS", "run.currentlyWorking": "CURRENTLY WORKING", "run.preparing": "Preparing desk...", "run.fieldNotes": "FIELD NOTES", "run.events": "{count} events", "run.elapsed": "Elapsed {time}", "run.deskStarted": "Desk started", "run.analystsAssigned": "{count} analysts assigned", "run.phaseStarted": "Phase started", "run.briefingReady": "Briefing ready", "run.reportGenerated": "Report generated", "run.signal": "Signal: {signal}", "run.graphUpdate": "Graph update",
      "phase.analyst": "Analyst Team", "phase.research": "Research Team", "phase.trading": "Trading Team", "phase.risk": "Risk Management", "phase.portfolio": "Portfolio Manager", "status.queued": "Queued", "status.running": "Running", "status.pending": "Queued", "status.in_progress": "In progress", "status.done": "Complete", "status.completed": "Complete", "status.failed": "Failed", "status.cancelled": "Cancelled", "status.report": "Report",
      "agent.market": "Market Analyst", "agent.social": "Sentiment Analyst", "agent.news": "News Analyst", "agent.fundamentals": "Fundamentals Analyst", "agent.bull": "Bull Researcher", "agent.bear": "Bear Researcher", "agent.manager": "Research Manager", "agent.trader": "Trader", "agent.aggressive": "Aggressive Analyst", "agent.conservative": "Conservative Analyst", "agent.neutral": "Neutral Analyst", "agent.portfolio": "Portfolio Manager",
      "report.finalBriefing": "FINAL BRIEFING", "report.decisionReport": "Decision report", "report.title": "{ticker} decision report", "report.signal": "Signal", "report.date": "Date", "report.asset": "Asset", "report.depth": "Depth", "report.provider": "Provider", "report.analysts": "I. Analyst Reports", "report.research": "II. Research Team Debate", "report.trading": "III. Trading Team Plan", "report.risk": "IV. Risk Management Team Decision", "report.portfolio": "V. Portfolio Manager Decision", "provider.local": "Local", "provider.configured": "configured",
      "error.ticker": "Enter a ticker symbol.", "error.analysts": "Select at least one analyst.", "error.validation": "Please check the highlighted fields.", "error.generic": "The request could not be completed.", "error.failed": "The analysis could not be completed.", "error.cancelled": "This analysis was cancelled before a report was generated.", "error.unavailable": "Unable to reach the local service.", "error.runConflict": "Another analysis is already running.", "error.runNotFound": "Run not found.", "error.reportNotFound": "Report not found.", "error.unsupportedTicker": "Ticker contains unsupported characters.", "error.effectiveAnalysts": "Select at least one analyst for this asset type."
    },
    zh: {
      "brand.localDesk": "本地研究台", "connection.ready": "就绪", "connection.live": "实时", "connection.reconnecting": "重连中", "connection.complete": "已完成", "connection.failed": "失败", "connection.cancelled": "已取消",
      "setup.kicker": "多智能体市场情报", "setup.titleLine1": "先明确问题。", "setup.titleLine2": "让研究台展开调查。", "setup.lede": "配置一次聚焦的研究运行，观察每位专家工作，并在一个界面获得可执行的简报。", "setup.credentials": "凭据保留在本地环境中。本研究台只展示非敏感的模型信息。", "setup.loadingConfig": "正在加载研究台配置……", "setup.configUnavailable": "本地配置不可用",
      "form.ticker": "标的代码", "form.tickerPlaceholder": "AAPL 或 BTC-USD", "form.assetType": "资产类型", "form.analysisDate": "分析日期", "form.analystDesk": "分析师团队", "form.researchDepth": "研究深度", "assets.stock": "股票", "assets.crypto": "加密货币", "analysts.market": "市场分析师", "analysts.social": "情绪分析师", "analysts.news": "新闻分析师", "analysts.fundamentals": "基本面分析师", "depth.quick": "快速", "depth.balanced": "均衡", "depth.deep": "深入",
      "actions.startAnalysis": "开始分析", "actions.refresh": "刷新", "actions.backHistory": "返回历史", "actions.cancelRun": "取消运行", "actions.downloadMarkdown": "下载 Markdown", "actions.newAnalysis": "新建分析", "actions.open": "打开",
      "history.kicker": "档案", "history.title": "历史简报", "history.loading": "正在加载历史记录……", "history.empty": "暂无已保存的简报。", "history.unavailable": "历史记录不可用。", "history.unknown": "未知",
      "run.liveBriefing": "实时简报", "run.inProgress": "分析进行中", "run.briefingTitle": "{ticker} 分析简报", "run.deskProgress": "研究台进度", "run.currentlyWorking": "当前工作代理", "run.preparing": "正在准备研究台……", "run.fieldNotes": "现场记录", "run.events": "{count} 条事件", "run.elapsed": "已用时 {time}", "run.deskStarted": "研究已开始", "run.analystsAssigned": "已分配 {count} 位分析师", "run.phaseStarted": "阶段开始", "run.briefingReady": "简报已生成", "run.reportGenerated": "报告已生成", "run.signal": "信号：{signal}", "run.graphUpdate": "图谱更新",
      "phase.analyst": "分析师团队", "phase.research": "研究团队", "phase.trading": "交易团队", "phase.risk": "风险管理", "phase.portfolio": "投资组合经理", "status.queued": "排队中", "status.running": "运行中", "status.pending": "排队中", "status.in_progress": "进行中", "status.done": "已完成", "status.completed": "已完成", "status.failed": "失败", "status.cancelled": "已取消", "status.report": "报告",
      "agent.market": "市场分析师", "agent.social": "情绪分析师", "agent.news": "新闻分析师", "agent.fundamentals": "基本面分析师", "agent.bull": "看多研究员", "agent.bear": "看空研究员", "agent.manager": "研究经理", "agent.trader": "交易员", "agent.aggressive": "激进分析师", "agent.conservative": "保守分析师", "agent.neutral": "中性分析师", "agent.portfolio": "投资组合经理",
      "report.finalBriefing": "最终简报", "report.decisionReport": "决策报告", "report.title": "{ticker} 决策报告", "report.signal": "信号", "report.date": "日期", "report.asset": "资产", "report.depth": "深度", "report.provider": "提供方", "report.analysts": "一、分析师报告", "report.research": "二、研究团队辩论", "report.trading": "三、交易团队计划", "report.risk": "四、风险管理团队决策", "report.portfolio": "五、投资组合经理决策", "provider.local": "本地", "provider.configured": "已配置",
      "error.ticker": "请输入标的代码。", "error.analysts": "至少选择一位分析师。", "error.validation": "请检查表单字段。", "error.generic": "请求未能完成。", "error.failed": "分析未能完成。", "error.cancelled": "分析已取消，未生成报告。", "error.unavailable": "无法连接本地服务。", "error.runConflict": "已有分析正在运行。", "error.runNotFound": "找不到该运行。", "error.reportNotFound": "找不到该报告。", "error.unsupportedTicker": "标的代码包含不支持的字符。", "error.effectiveAnalysts": "请为此资产类型至少选择一位分析师。"
    }
  };
  const PHASE_KEYS = ["analyst", "research", "trading", "risk", "portfolio"];
  const REPORT_GROUPS = [["report.analysts", "analysts", [["agent.market", "market"], ["agent.social", "sentiment"], ["agent.news", "news"], ["agent.fundamentals", "fundamentals"]]], ["report.research", "research", [["agent.bull", "bull"], ["agent.bear", "bear"], ["agent.manager", "manager"]]], ["report.trading", "trading", [["agent.trader", "trader"]]], ["report.risk", "risk", [["agent.aggressive", "aggressive"], ["agent.conservative", "conservative"], ["agent.neutral", "neutral"]]], ["report.portfolio", "portfolio", [["agent.portfolio", "decision"]]]];
  const AGENT_KEYS = { "Market Analyst": "agent.market", "Sentiment Analyst": "agent.social", "News Analyst": "agent.news", "Fundamentals Analyst": "agent.fundamentals", "Bull Researcher": "agent.bull", "Bear Researcher": "agent.bear", "Research Manager": "agent.manager", Trader: "agent.trader", "Aggressive Analyst": "agent.aggressive", "Conservative Analyst": "agent.conservative", "Neutral Analyst": "agent.neutral", "Portfolio Manager": "agent.portfolio" };
  const PHASE_NAME_KEYS = { "Analyst Team": "phase.analyst", "Research Team": "phase.research", "Trading Team": "phase.trading", "Risk Management": "phase.risk", "Portfolio Manager": "phase.portfolio" };
  const DYNAMIC_KEYS = { graph: "run.graphUpdate" };
  const state = { language: "en", runId: null, lastSeq: 0, seen: new Set(), source: null, reportId: null, runRecord: null, elapsedTimer: null, startedAt: null, provider: null, config: null, phases: PHASE_KEYS.map((key) => ({ key, status: "pending" })), history: [] };
  const $ = (id) => document.getElementById(id);
  const setupView = $("setup-view"), runView = $("run-view"), form = $("analysis-form");
  function t(key, vars = {}) { const value = I18N[state.language][key] || I18N.en[key] || key; return value.replace(/\{(\w+)\}/g, (_, name) => String(vars[name] ?? "")); }
  function translateDynamic(value) { return t(AGENT_KEYS[value] || PHASE_NAME_KEYS[value] || DYNAMIC_KEYS[value] || value); }
  function applyTranslations() { document.documentElement.lang = state.language === "zh" ? "zh-CN" : "en-US"; document.title = state.language === "zh" ? "TradingAgents | 分析研究台" : "TradingAgents | Analysis Desk"; document.querySelectorAll("[data-i18n]").forEach((node) => { node.textContent = t(node.dataset.i18n); }); document.querySelectorAll("[data-i18n-placeholder]").forEach((node) => { node.placeholder = t(node.dataset.i18nPlaceholder); }); if (state.config) { renderFormOptions(state.config); toggleCryptoAnalyst(); } $("language-toggle").textContent = state.language === "zh" ? "English" : "简体中文"; $("language-toggle").setAttribute("aria-label", state.language === "zh" ? "切换语言" : "Switch language"); renderPhases(); if (state.history.length) renderHistory(state.history); if (state.runRecord) showRun(state.runRecord); }
  function setLanguage(language) { state.language = language === "zh" ? "zh" : "en"; localStorage.setItem("tradingagents-language", state.language); applyTranslations(); }
  function setConnection(key) { const node = $("connection-status"); node.textContent = t(`connection.${key}`); node.className = `connection-dot is-${key === "live" || key === "complete" ? "live" : key === "ready" ? "idle" : "error"}`; }
  function escapeHtml(value) { return String(value ?? "").replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[c])); }
  function renderMarkdown(source) { let text = escapeHtml(source); text = text.replace(/```[\s\S]*?```/g, (block) => `<pre>${block.slice(3, -3).replace(/^\w+\n/, "")}</pre>`); text = text.replace(/^### (.+)$/gm, "<h3>$1</h3>").replace(/^## (.+)$/gm, "<h2>$1</h2>").replace(/^# (.+)$/gm, "<h1>$1</h1>"); text = text.replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>").replace(/\*([^*]+)\*/g, "<em>$1</em>"); return text.split(/\n{2,}/).map((part) => { if (/^<h[123]>/.test(part) || /^<pre>/.test(part)) return part; const lines = part.split("\n"); if (lines.every((line) => /^[-*] /.test(line))) return `<ul>${lines.map((line) => `<li>${line.slice(2)}</li>`).join("")}</ul>`; return `<p>${part.replace(/\n/g, "<br />")}</p>`; }).join(""); }
  function localizeError(message) { const text = String(message || ""); if (/already active/i.test(text)) return t("error.runConflict"); if (/run not found/i.test(text)) return t("error.runNotFound"); if (/report not found/i.test(text)) return t("error.reportNotFound"); if (/unsupported characters/i.test(text)) return t("error.unsupportedTicker"); if (/after asset filtering/i.test(text)) return t("error.effectiveAnalysts"); if (/analysis worker failed/i.test(text)) return t("error.failed"); return text || t("error.generic"); }
  function api(path, options) { return fetch(path, { headers: { Accept: "application/json", ...(options && options.headers) }, ...options }).then(async (response) => { const body = await response.json().catch(() => ({})); if (!response.ok) { if (response.status === 422) throw new Error(t("error.validation")); throw new Error(localizeError(body.detail)); } return body; }).catch((error) => { if (error instanceof TypeError) throw new Error(t("error.unavailable")); throw error; }); }
  function formatElapsed(startedAt) { const seconds = Math.max(0, Math.floor((Date.now() - new Date(startedAt).getTime()) / 1000)); return t("run.elapsed", { time: `${String(Math.floor(seconds / 60)).padStart(2, "0")}:${String(seconds % 60).padStart(2, "0")}` }); }
  function startElapsed(startedAt) { state.startedAt = startedAt || new Date().toISOString(); clearInterval(state.elapsedTimer); $("elapsed-time").textContent = formatElapsed(state.startedAt); state.elapsedTimer = setInterval(() => { if (state.startedAt) $("elapsed-time").textContent = formatElapsed(state.startedAt); }, 1000); }
  function stopElapsed() { clearInterval(state.elapsedTimer); state.elapsedTimer = null; }
  function resetRunState() { stopElapsed(); state.lastSeq = 0; state.seen = new Set(); state.reportId = null; state.runRecord = null; state.startedAt = null; state.phases = PHASE_KEYS.map((key) => ({ key, status: "pending" })); $("activity-feed").innerHTML = ""; $("terminal-panel").hidden = true; $("report-panel").hidden = true; $("progress-bar").style.width = "0%"; $("progress-label").textContent = "0%"; $("event-count").textContent = t("run.events", { count: 0 }); $("elapsed-time").textContent = t("run.elapsed", { time: "00:00" }); renderPhases(); }
  function showSetup() { stopElapsed(); if (state.source) state.source.close(); state.source = null; state.runId = null; runView.hidden = true; setupView.hidden = false; setConnection("ready"); api("/api/history").then(renderHistory).catch(() => {}); }
  function showRun(record) { state.runRecord = record; setupView.hidden = true; runView.hidden = false; $("run-title").textContent = t("run.briefingTitle", { ticker: record.request.ticker }); $("run-subtitle").textContent = `${record.request.analysis_date} · ${record.request.asset_type === "crypto" ? t("assets.crypto") : t("assets.stock")} · ${t("form.researchDepth")} ${record.request.research_depth}`; startElapsed(record.started_at || new Date().toISOString()); }
  function renderPhases() { $("phase-timeline").innerHTML = state.phases.map((phase) => `<li class="is-${phase.status}"><span class="phase-dot"></span><div><span class="phase-name">${escapeHtml(t(`phase.${phase.key}`))}</span><span class="phase-status">${escapeHtml(t(`status.${phase.status}`))}</span></div></li>`).join(""); }
  function addActivity(title, summary, timestamp) { const feed = $("activity-feed"); const entry = document.createElement("article"); entry.className = "activity-entry"; entry.innerHTML = `<time>${escapeHtml(new Date(timestamp || Date.now()).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }))}</time><strong>${escapeHtml(translateDynamic(title))}</strong><p>${escapeHtml(summary)}</p>`; feed.appendChild(entry); feed.scrollTop = feed.scrollHeight; $("event-count").textContent = t("run.events", { count: feed.children.length }); }
  function updatePhase(name) { const key = PHASE_NAME_KEYS[name]?.replace("phase.", "") || name; const index = PHASE_KEYS.indexOf(key); if (index < 0) return; state.phases = state.phases.map((phase, i) => ({ key: phase.key, status: i < index ? "done" : i === index ? "in_progress" : "pending" })); renderPhases(); }
  function processEvent(envelope) { if (!envelope || envelope.run_id !== state.runId || state.seen.has(envelope.seq)) return; state.seen.add(envelope.seq); state.lastSeq = Math.max(state.lastSeq, Number(envelope.seq) || 0); const payload = envelope.payload || {}; switch (envelope.event) { case "run_started": setConnection("live"); startElapsed(state.runRecord?.started_at || envelope.timestamp); addActivity("run.deskStarted", t("run.analystsAssigned", { count: payload.analysts?.length || 0 }), envelope.timestamp); break; case "phase_changed": updatePhase(payload.phase); addActivity(payload.phase, payload.status === "in_progress" ? t("run.phaseStarted") : t(`status.${payload.status}`), envelope.timestamp); break; case "agent_status": if (payload.status === "in_progress") $("current-agent").textContent = translateDynamic(payload.agent); addActivity(payload.agent, t(`status.${payload.status}`), envelope.timestamp); break; case "progress": { const percent = Math.round((Number(payload.progress) || 0) * 100); $("progress-bar").style.width = `${percent}%`; $("progress-label").textContent = `${percent}%`; if (payload.current_agent) $("current-agent").textContent = translateDynamic(payload.current_agent); break; } case "message": addActivity(t("run.fieldNotes"), payload.text || "", envelope.timestamp); break; case "activity": addActivity(payload.name || t("run.graphUpdate"), payload.summary || "", envelope.timestamp); break; case "run_completed": completeRun(payload); break; case "run_failed": terminalRun("failed", localizeError(payload.error_message) || t("error.failed")); break; case "run_cancelled": terminalRun("cancelled", t("error.cancelled")); break; case "run_snapshot": if (payload.run) syncRecord(payload.run); break; default: break; } }
  function syncRecord(record) { state.runRecord = record; if (record.started_at) startElapsed(record.started_at); if (record.status === "completed" && record.report_id) loadReport(record.report_id); else if (["failed", "cancelled"].includes(record.status)) terminalRun(record.status, record.error_message || t(`error.${record.status}`)); }
  function reconnectStatus() { if (!state.runId) return Promise.resolve(); return api(`/api/runs/${encodeURIComponent(state.runId)}`).then((record) => { syncRecord(record); return record; }).catch(() => null); }
  function connectEvents() { if (!state.runId) return; if (state.source) state.source.close(); const url = `/api/runs/${encodeURIComponent(state.runId)}/events?after_seq=${state.lastSeq}`; const source = new EventSource(url); state.source = source; source.onopen = () => setConnection("live"); source.onmessage = (event) => { try { processEvent(JSON.parse(event.data)); } catch (_) {} }; ["run_snapshot", "run_started", "phase_changed", "agent_status", "progress", "message", "activity", "run_completed", "run_failed", "run_cancelled"].forEach((name) => source.addEventListener(name, (event) => { try { processEvent(JSON.parse(event.data)); } catch (_) {} })); source.onerror = () => { setConnection("reconnecting"); reconnectStatus().then((record) => { if (record && ["completed", "failed", "cancelled"].includes(record.status)) { source.close(); state.source = null; return; } if (source.readyState === EventSource.CLOSED) { source.close(); setTimeout(connectEvents, 1000); } }); }; }
  function completeRun(payload) { stopElapsed(); state.reportId = payload.report_id; $("cancel-run").hidden = true; $("new-analysis").hidden = false; $("progress-bar").style.width = "100%"; $("progress-label").textContent = "100%"; state.phases = state.phases.map((phase) => ({ ...phase, status: "done" })); renderPhases(); setConnection("complete"); addActivity(t("run.briefingReady"), payload.signal ? t("run.signal", { signal: payload.signal }) : t("run.reportGenerated"), Date.now()); loadReport(payload.report_id); }
  function terminalRun(status, message) { stopElapsed(); $("cancel-run").hidden = true; $("new-analysis").hidden = false; $("terminal-panel").hidden = false; $("terminal-panel").innerHTML = `<h3>${escapeHtml(status === "failed" ? t("error.failed") : t("connection.cancelled"))}</h3><p>${escapeHtml(message)}</p>`; setConnection(status); }
  async function loadReport(reportId) { try { const report = await api(`/api/history/${encodeURIComponent(reportId)}`); renderReport(report); } catch (error) { terminalRun("failed", error.message); } }
  function renderReport(report) { $("report-panel").hidden = false; $("report-title").textContent = t("report.title", { ticker: report.ticker || t("report.decisionReport") }); const metadata = [["report.signal", report.signal], ["report.date", report.analysis_date], ["report.asset", report.asset_type === "crypto" ? t("assets.crypto") : report.asset_type === "stock" ? t("assets.stock") : report.asset_type], ["report.depth", state.runRecord?.request?.research_depth], ["report.provider", state.provider]].filter(([, value]) => value !== null && value !== undefined && value !== ""); $("report-meta").innerHTML = metadata.map(([label, value]) => `<div><dt>${escapeHtml(t(label))}</dt><dd>${escapeHtml(value)}</dd></div>`).join(""); $("download-report").onclick = () => { window.location.href = `/api/history/${encodeURIComponent(report.report_id)}/download`; }; $("report-content").innerHTML = REPORT_GROUPS.map(([title, group, sections]) => { const blocks = sections.map(([label, key]) => report[group]?.[key] ? `<article class="report-section"><h3>${escapeHtml(t(label))}</h3>${renderMarkdown(report[group][key])}</article>` : "").join(""); return blocks ? `<section><h2>${escapeHtml(t(title))}</h2>${blocks}</section>` : ""; }).join(""); }
  function renderHistory(records) { state.history = records; const list = $("history-list"); if (!records.length) { list.innerHTML = `<p class="muted">${escapeHtml(t("history.empty"))}</p>`; return; } list.innerHTML = records.map((record) => `<article class="history-item"><button type="button" data-report-id="${escapeHtml(record.report_id)}"><span class="history-ticker">${escapeHtml(record.ticker || t("history.unknown"))}</span><span class="history-meta">${escapeHtml(record.analysis_date || "")}</span></button><span class="history-signal">${escapeHtml(record.signal || t(`status.${record.status}`) || t("status.report"))}</span><span class="history-preview">${escapeHtml(record.decision_preview || "")}</span><button class="text-button" type="button" data-report-id="${escapeHtml(record.report_id)}">${escapeHtml(t("actions.open"))}</button></article>`).join(""); list.querySelectorAll("[data-report-id]").forEach((button) => button.addEventListener("click", () => reopenHistory(button.dataset.reportId))); }
  async function reopenHistory(reportId) { resetRunState(); showRun({ request: { ticker: t("history.title"), analysis_date: "", asset_type: "", research_depth: "" } }); $("cancel-run").hidden = true; await loadReport(reportId); }
  function renderFormOptions(config) { const selectedAnalysts = new Set([...form.querySelectorAll('input[name="analysts"]:checked')].map((input) => input.value)); const analystDefaults = selectedAnalysts.size ? selectedAnalysts : new Set((config.analyst_options || []).slice(0, 2).map((item) => item.key)); const selectedDepth = form.querySelector('input[name="research_depth"]:checked')?.value; $("analyst-options").innerHTML = (config.analyst_options || []).map((item) => `<div class="choice"><input id="analyst-${escapeHtml(item.key)}" type="checkbox" name="analysts" value="${escapeHtml(item.key)}" ${analystDefaults.has(item.key) ? "checked" : ""} /><label for="analyst-${escapeHtml(item.key)}">${escapeHtml(t(`analysts.${item.key}`))}</label></div>`).join(""); $("depth-options").innerHTML = (config.research_depths || [1, 3, 5]).map((depth, index) => `<div class="choice"><input id="depth-${depth}" type="radio" name="research_depth" value="${depth}" ${String(depth) === String(selectedDepth ?? (config.research_depths || [1])[0]) || (!selectedDepth && index === 0) ? "checked" : ""} /><label for="depth-${depth}">${escapeHtml(t(depth === 1 ? "depth.quick" : depth === 3 ? "depth.balanced" : "depth.deep"))}</label></div>`).join(""); }
  function setupForm(config) { state.config = config; state.provider = `${config.provider || t("provider.local")} · ${config.model || t("provider.configured")}`; $("analysis-date").value = config.default_date || new Date().toISOString().slice(0, 10); $("provider-summary").textContent = state.provider; renderFormOptions(config); $("asset-type").addEventListener("change", toggleCryptoAnalyst); toggleCryptoAnalyst(); }
  function toggleCryptoAnalyst() { const disabled = $("asset-type").value === "crypto"; const fundamentals = $("analyst-fundamentals"); if (!fundamentals) return; fundamentals.checked = false; fundamentals.disabled = disabled; fundamentals.closest(".choice").classList.toggle("is-disabled", disabled); }
  async function submitRun(event) { event.preventDefault(); $("form-error").textContent = ""; const analysts = [...form.querySelectorAll('input[name="analysts"]:checked')].map((input) => input.value); if (!$("ticker").value.trim()) { $("form-error").textContent = t("error.ticker"); return; } if (!analysts.length) { $("form-error").textContent = t("error.analysts"); return; } const body = { ticker: $("ticker").value.trim(), analysis_date: $("analysis-date").value, asset_type: $("asset-type").value, analysts, research_depth: Number(form.querySelector('input[name="research_depth"]:checked')?.value || 1) }; try { const record = await api("/api/runs", { method: "POST", body: JSON.stringify(body), headers: { "Content-Type": "application/json" } }); resetRunState(); state.runId = record.run_id; showRun(record); $("cancel-run").hidden = false; $("new-analysis").hidden = true; connectEvents(); } catch (error) { $("form-error").textContent = localizeError(error.message); } }
  $("language-toggle").addEventListener("click", () => setLanguage(state.language === "zh" ? "en" : "zh")); $("cancel-run").addEventListener("click", async () => { if (!state.runId) return; try { await api(`/api/runs/${encodeURIComponent(state.runId)}/cancel`, { method: "POST" }); } catch (error) { terminalRun("failed", error.message); } }); $("back-history").addEventListener("click", showSetup); $("new-analysis").addEventListener("click", showSetup); $("refresh-history").addEventListener("click", () => api("/api/history").then(renderHistory).catch(() => {})); form.addEventListener("submit", submitRun);
  const savedLanguage = localStorage.getItem("tradingagents-language"); state.language = savedLanguage || ((navigator.language || "").toLowerCase().startsWith("zh") ? "zh" : "en"); applyTranslations(); renderPhases(); api("/api/config").then(setupForm).catch(() => { $("provider-summary").textContent = t("setup.configUnavailable"); }); api("/api/history").then(renderHistory).catch(() => { $("history-list").innerHTML = `<p class="muted">${escapeHtml(t("history.unavailable"))}</p>`; });
})();
