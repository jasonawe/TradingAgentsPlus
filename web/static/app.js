(() => {
  "use strict";

  const I18N = {
    en: {
      "brand.localDesk": "LOCAL RESEARCH DESK",
      "connection.ready": "Ready", "connection.live": "Live", "connection.reconnecting": "Reconnecting",
      "connection.complete": "Complete", "connection.failed": "Failed", "connection.cancelled": "Cancelled", "connection.interrupted": "Interrupted",
      "setup.kicker": "MULTI-AGENT MARKET INTELLIGENCE", "setup.titleLine1": "Frame the question.", "setup.titleLine2": "Let the desk investigate.",
      "setup.lede": "Configure a focused research run, watch each specialist work, and receive a decision-ready briefing in one place.",
      "setup.credentials": "Credentials stay in your local environment. This desk only exposes non-sensitive model metadata.", "setup.loadingConfig": "Loading desk configuration...", "setup.configUnavailable": "Local configuration unavailable",
      "form.ticker": "Ticker", "form.tickerPlaceholder": "AAPL or BTC-USD", "form.assetType": "Asset type", "form.analysisDate": "Analysis date", "form.provider": "Model provider", "form.quickModel": "Quick-thinking model", "form.deepModel": "Deep-thinking model", "form.outputLanguage": "Analysis language", "form.analystDesk": "Analyst desk", "form.researchDepth": "Research depth",
      "assets.stock": "Stock", "assets.crypto": "Crypto", "analysts.market": "Market Analyst", "analysts.social": "Sentiment Analyst", "analysts.news": "News Analyst", "analysts.fundamentals": "Fundamentals Analyst", "depth.quick": "Quick", "depth.balanced": "Balanced", "depth.deep": "Deep",
      "actions.startAnalysis": "Start analysis", "actions.refresh": "Refresh", "actions.openLibrary": "Report library", "actions.backHistory": "Back to history", "actions.cancelRun": "Cancel run", "actions.downloadMarkdown": "Download markdown", "actions.newAnalysis": "New analysis", "actions.open": "Open",
      "history.kicker": "ARCHIVE", "history.title": "Previous briefings", "history.loading": "Loading history...", "history.empty": "No briefings saved yet.", "history.unavailable": "History unavailable.", "history.unknown": "Unknown",
      "library.kicker": "REPORT LIBRARY", "library.title": "Your research archive", "library.lede": "Search and reopen reports generated by this local desk.", "library.searchLabel": "Search reports", "library.searchPlaceholder": "Ticker or decision text", "library.assetFilter": "Asset", "library.statusFilter": "Status", "library.sort": "Sort", "library.allAssets": "All assets", "library.allStatuses": "All statuses", "library.newest": "Newest first", "library.oldest": "Oldest first", "library.noMatches": "No reports match these filters.", "library.legacy": "Legacy report", "library.metadataUnavailable": "Metadata unavailable",
      "run.liveBriefing": "LIVE BRIEFING", "run.inProgress": "Analysis in progress", "run.briefingTitle": "{ticker} briefing", "run.deskProgress": "DESK PROGRESS", "run.currentlyWorking": "CURRENTLY WORKING", "run.preparing": "Preparing desk...", "run.fieldNotes": "FIELD NOTES", "run.events": "{count} events", "run.elapsed": "Elapsed {time}", "run.deskStarted": "Desk started", "run.analystsAssigned": "{count} analysts assigned", "run.phaseStarted": "Phase started", "run.briefingReady": "Briefing ready", "run.reportGenerated": "Report generated", "run.signal": "Signal: {signal}", "run.graphUpdate": "Graph update",
      "phase.analyst": "Analyst Team", "phase.research": "Research Team", "phase.trading": "Trading Team", "phase.risk": "Risk Management", "phase.portfolio": "Portfolio Manager", "status.queued": "Queued", "status.running": "Running", "status.pending": "Queued", "status.in_progress": "In progress", "status.done": "Complete", "status.completed": "Complete", "status.failed": "Failed", "status.cancelled": "Cancelled", "status.report": "Report",
      "agent.market": "Market Analyst", "agent.social": "Sentiment Analyst", "agent.news": "News Analyst", "agent.fundamentals": "Fundamentals Analyst", "agent.bull": "Bull Researcher", "agent.bear": "Bear Researcher", "agent.manager": "Research Manager", "agent.trader": "Trader", "agent.aggressive": "Aggressive Analyst", "agent.conservative": "Conservative Analyst", "agent.neutral": "Neutral Analyst", "agent.portfolio": "Portfolio Manager",
      "report.finalBriefing": "FINAL BRIEFING", "report.executiveSummary": "EXECUTIVE SUMMARY", "report.deepModelSummary": "Deep model synthesis", "report.showDetails": "Show detailed agent reports", "report.decisionReport": "Decision report", "report.title": "{ticker} decision report", "report.signal": "Signal", "report.date": "Date", "report.asset": "Asset", "report.depth": "Depth", "report.provider": "Provider", "report.quickModel": "Quick model", "report.deepModel": "Deep model", "report.language": "Language", "report.source": "Source", "report.analysts": "I. Analyst Reports", "report.research": "II. Research Team Debate", "report.trading": "III. Trading Team Plan", "report.risk": "IV. Risk Management Team Decision", "report.portfolio": "V. Portfolio Manager Decision", "provider.local": "Local", "provider.configured": "configured",
      "error.ticker": "Enter a ticker symbol.", "error.analysts": "Select at least one analyst.", "error.validation": "Please check the highlighted fields.", "error.invalidConfig": "Select a valid provider, model, and output language.", "error.generic": "The request could not be completed.", "error.failed": "The analysis could not be completed.", "error.cancelled": "This analysis was cancelled before a report was generated.", "error.unavailable": "Unable to reach the local service.", "error.runConflict": "Another analysis is already running.", "error.runNotFound": "Run not found.", "error.reportNotFound": "Report not found.", "error.unsupportedTicker": "Ticker contains unsupported characters.", "error.effectiveAnalysts": "Select at least one analyst for this asset type."
    },
    zh: {
      "brand.localDesk": "本地研究台", "connection.ready": "就绪", "connection.live": "实时", "connection.reconnecting": "重连中", "connection.complete": "已完成", "connection.failed": "失败", "connection.cancelled": "已取消", "connection.interrupted": "已中断",
      "setup.kicker": "多智能体市场情报", "setup.titleLine1": "先明确问题。", "setup.titleLine2": "让研究台展开调查。", "setup.lede": "配置一次聚焦的研究运行，观察每位专家工作，并在一个界面获得可执行的简报。", "setup.credentials": "凭据保留在本地环境中。本研究台只展示非敏感的模型信息。", "setup.loadingConfig": "正在加载研究台配置……", "setup.configUnavailable": "本地配置不可用",
      "form.ticker": "标的代码", "form.tickerPlaceholder": "AAPL 或 BTC-USD", "form.assetType": "资产类型", "form.analysisDate": "分析日期", "form.provider": "模型厂商", "form.quickModel": "快速思考模型", "form.deepModel": "深度思考模型", "form.outputLanguage": "分析语言", "form.analystDesk": "分析师团队", "form.researchDepth": "研究深度", "assets.stock": "股票", "assets.crypto": "加密货币", "analysts.market": "市场分析师", "analysts.social": "情绪分析师", "analysts.news": "新闻分析师", "analysts.fundamentals": "基本面分析师", "depth.quick": "快速", "depth.balanced": "均衡", "depth.deep": "深入",
      "actions.startAnalysis": "开始分析", "actions.refresh": "刷新", "actions.openLibrary": "报告库", "actions.backHistory": "返回历史", "actions.cancelRun": "取消运行", "actions.downloadMarkdown": "下载 Markdown", "actions.newAnalysis": "新建分析", "actions.open": "打开",
      "history.kicker": "档案", "history.title": "历史简报", "history.loading": "正在加载历史记录……", "history.empty": "暂无已保存的简报。", "history.unavailable": "历史记录不可用。", "history.unknown": "未知",
      "library.kicker": "报告库", "library.title": "研究档案", "library.lede": "搜索并重新打开本地研究台生成的报告。", "library.searchLabel": "搜索报告", "library.searchPlaceholder": "标的代码或决策文本", "library.assetFilter": "资产", "library.statusFilter": "状态", "library.sort": "排序", "library.allAssets": "全部资产", "library.allStatuses": "全部状态", "library.newest": "最新优先", "library.oldest": "最早优先", "library.noMatches": "没有符合筛选条件的报告。", "library.legacy": "历史报告", "library.metadataUnavailable": "元数据不可用",
      "run.liveBriefing": "实时分析", "run.inProgress": "分析进行中", "run.briefingTitle": "{ticker} 分析简报", "run.deskProgress": "分析进度", "run.currentlyWorking": "当前工作代理", "run.preparing": "正在准备分析……", "run.fieldNotes": "过程记录", "run.events": "{count} 条事件", "run.elapsed": "已用时 {time}", "run.deskStarted": "分析已开始", "run.analystsAssigned": "已分配 {count} 位分析师", "run.phaseStarted": "阶段开始", "run.briefingReady": "简报已生成", "run.reportGenerated": "报告已生成", "run.signal": "信号：{signal}", "run.graphUpdate": "图谱更新",
      "phase.analyst": "分析师团队", "phase.research": "研究团队", "phase.trading": "交易团队", "phase.risk": "风险管理", "phase.portfolio": "投资组合经理", "status.queued": "排队中", "status.running": "运行中", "status.pending": "排队中", "status.in_progress": "进行中", "status.done": "已完成", "status.completed": "已完成", "status.failed": "失败", "status.cancelled": "已取消", "status.interrupted": "已中断", "status.report": "报告",
      "agent.market": "市场分析师", "agent.social": "情绪分析师", "agent.news": "新闻分析师", "agent.fundamentals": "基本面分析师", "agent.bull": "看多研究员", "agent.bear": "看空研究员", "agent.manager": "研究经理", "agent.trader": "交易员", "agent.aggressive": "激进分析师", "agent.conservative": "保守分析师", "agent.neutral": "中性分析师", "agent.portfolio": "投资组合经理",
      "report.finalBriefing": "最终报告", "report.executiveSummary": "综合研判", "report.deepModelSummary": "深度模型综合", "report.showDetails": "展开详细智能体报告", "report.decisionReport": "决策报告", "report.title": "{ticker} 决策报告", "report.signal": "评级", "report.date": "日期", "report.asset": "资产", "report.depth": "深度", "report.provider": "提供方", "report.quickModel": "快速模型", "report.deepModel": "深度模型", "report.language": "语言", "report.source": "来源", "report.dataStatus": "数据状态", "report.snapshot": "数据快照", "report.analysts": "一、分析师报告", "report.research": "二、研究团队辩论", "report.trading": "三、交易团队计划", "report.risk": "四、风险管理团队决策", "report.portfolio": "五、投资组合经理决策", "provider.local": "本地", "provider.configured": "已配置",
      "error.ticker": "请输入标的代码。", "error.analysts": "至少选择一位分析师。", "error.validation": "请检查表单字段。", "error.invalidConfig": "请选择有效的模型厂商、模型和输出语言。", "error.generic": "请求未能完成。", "error.failed": "分析未能完成。", "error.cancelled": "分析已取消，未生成报告。", "error.interrupted": "分析被中断，未生成报告。", "error.unavailable": "无法连接本地服务。", "error.runConflict": "已有分析正在运行。", "error.runNotFound": "找不到该运行。", "error.reportNotFound": "找不到该报告。", "error.unsupportedTicker": "标的代码包含不支持的字符。", "error.effectiveAnalysts": "请为此资产类型至少选择一位分析师。"
    }
  };
  const PHASE_KEYS = ["analyst", "research", "trading", "risk", "portfolio"];
  const REPORT_GROUPS = [["report.analysts", "analysts", [["agent.market", "market"], ["agent.social", "sentiment"], ["agent.news", "news"], ["agent.fundamentals", "fundamentals"]]], ["report.research", "research", [["agent.bull", "bull"], ["agent.bear", "bear"], ["agent.manager", "manager"]]], ["report.trading", "trading", [["agent.trader", "trader"]]], ["report.risk", "risk", [["agent.aggressive", "aggressive"], ["agent.conservative", "conservative"], ["agent.neutral", "neutral"]]], ["report.portfolio", "portfolio", [["agent.portfolio", "decision"]]]];
  const AGENT_KEYS = { "Market Analyst": "agent.market", "Sentiment Analyst": "agent.social", "News Analyst": "agent.news", "Fundamentals Analyst": "agent.fundamentals", "Bull Researcher": "agent.bull", "Bear Researcher": "agent.bear", "Research Manager": "agent.manager", Trader: "agent.trader", "Aggressive Analyst": "agent.aggressive", "Conservative Analyst": "agent.conservative", "Neutral Analyst": "agent.neutral", "Portfolio Manager": "agent.portfolio" };
  const PHASE_NAME_KEYS = { "Analyst Team": "phase.analyst", "Research Team": "phase.research", "Trading Team": "phase.trading", "Risk Management": "phase.risk", "Portfolio Manager": "phase.portfolio" };
  const DYNAMIC_KEYS = { graph: "run.graphUpdate" };
  const LANGUAGE_KEYS = { English: "English", Chinese: "中文", Japanese: "日本語", Korean: "한국어", Hindi: "हिन्दी", Spanish: "Español", Portuguese: "Português", French: "Français", German: "Deutsch", Arabic: "العربية", Russian: "Русский" };
  const SETTINGS_LABELS = { quote_strategy_id: "行情策略", quote_provider_chain: "行情来源链", quote_ttl_seconds: "行情缓存秒数", output_language: "默认分析语言", effective_output_language: "生效分析语言" };
  const state = { language: "zh", runId: null, lastSeq: 0, seen: new Set(), source: null, reportId: null, runRecord: null, elapsedTimer: null, startedAt: null, config: null, archived: false, history: [], watchlist: { version: 1, items: [] }, filters: { search: "", asset: "", status: "", sort: "newest" } , phases: PHASE_KEYS.map((key) => ({ key, status: "pending" })) };
  const ACTIVE_RUN_KEY = "tradingagents-active-run";
  const $ = (id) => document.getElementById(id);
  const setupView = $("setup-view"), libraryView = $("library-view"), settingsView = $("settings-view"), runView = $("run-view"), form = $("analysis-form");
  function t(key, vars = {}) { const value = I18N[state.language][key] || I18N.en[key] || key; return value.replace(/\{(\w+)\}/g, (_, name) => String(vars[name] ?? "")); }
  function translateDynamic(value) { return t(AGENT_KEYS[value] || PHASE_NAME_KEYS[value] || DYNAMIC_KEYS[value] || value); }
  function languageLabel(value) { return LANGUAGE_KEYS[value] || value || t("library.metadataUnavailable"); }
  function applyTranslations() { document.documentElement.lang = "zh-CN"; document.title = "TradingAgents | 个人投资研究平台"; document.querySelectorAll("[data-i18n]").forEach((node) => { node.textContent = t(node.dataset.i18n); }); document.querySelectorAll("[data-i18n-placeholder]").forEach((node) => { node.placeholder = t(node.dataset.i18nPlaceholder); }); if (state.config) { renderFormOptions(state.config); toggleCryptoAnalyst(); } renderPhases(); renderHistory(state.history); if (!libraryView.hidden) renderLibrary(); if (state.runRecord) renderRunHeader(state.runRecord); }
  function setLanguage() { state.language = "zh"; applyTranslations(); }
  function setConnection(key) { const node = $("connection-status"); node.textContent = t(`connection.${key}`); node.className = `connection-dot is-${key === "live" || key === "complete" ? "live" : key === "ready" ? "idle" : "error"}`; }
  function escapeHtml(value) { return String(value ?? "").replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#039;" }[c])); }
  function renderMarkdown(source) { const lines = String(source ?? "").replace(/\r\n?/g, "\n").split("\n"); const output = []; const inline = (value) => { let text = escapeHtml(value); text = text.replace(/`([^`]+)`/g, "<code>$1</code>").replace(/\[([^\]]+)\]\((https?:\/\/[^)\s]+)\)/g, '<a href="$2" target="_blank" rel="noreferrer">$1</a>').replace(/\*\*(.+?)\*\*/g, "<strong>$1</strong>").replace(/\*([^*]+)\*/g, "<em>$1</em>"); return text; }; const row = (line) => { let value = line.trim(); if (value.startsWith("|")) value = value.slice(1); if (value.endsWith("|")) value = value.slice(0, -1); return value.split("|").map((cell) => cell.trim()); }; const separator = (cells) => cells.length > 0 && cells.every((cell) => /^:?-{3,}:?$/.test(cell)); const table = (index) => { if (index + 1 >= lines.length || !lines[index].includes("|") || !separator(row(lines[index + 1]))) return null; const headers = row(lines[index]); const rules = row(lines[index + 1]); const alignments = rules.map((cell) => cell.startsWith(":") && cell.endsWith(":") ? "center" : cell.startsWith(":") ? "left" : cell.endsWith(":") ? "right" : "left"); const cells = (values, tag) => headers.map((_, position) => `<${tag} style="text-align:${alignments[position] || "left"}">${inline(values[position] || "")}</${tag}>`).join(""); let html = `<div class="table-wrap"><table><thead><tr>${cells(headers, "th")}</tr></thead><tbody>`; let cursor = index + 2; while (cursor < lines.length && lines[cursor].trim() && lines[cursor].includes("|")) { const values = row(lines[cursor]); html += `<tr>${headers.map((_, position) => `<td style="text-align:${alignments[position] || "left"}">${inline(values[position] || "")}</td>`).join("")}</tr>`; cursor += 1; } return { html: `${html}</tbody></table></div>`, next: cursor }; }; const special = (value, index) => /^#{1,3}\s+/.test(value) || /^```/.test(value) || /^\s*[-*+]\s+/.test(value) || /^\s*\d+\.\s+/.test(value) || /^\s*>/.test(value) || table(index) !== null; let index = 0; while (index < lines.length) { const current = lines[index]; if (!current.trim()) { index += 1; continue; } if (/^```/.test(current)) { const code = []; index += 1; while (index < lines.length && !/^```/.test(lines[index])) code.push(lines[index++]); if (index < lines.length) index += 1; output.push(`<pre><code>${escapeHtml(code.join("\n"))}</code></pre>`); continue; } const parsedTable = table(index); if (parsedTable) { output.push(parsedTable.html); index = parsedTable.next; continue; } const heading = current.match(/^(#{1,3})\s+(.+)$/); if (heading) { output.push(`<h${heading[1].length}>${inline(heading[2])}</h${heading[1].length}>`); index += 1; continue; } if (/^\s*[-*+]\s+/.test(current)) { const items = []; while (index < lines.length && /^\s*[-*+]\s+/.test(lines[index])) items.push(`<li>${inline(lines[index].replace(/^\s*[-*+]\s+/, ""))}</li>`), index += 1; output.push(`<ul>${items.join("")}</ul>`); continue; } if (/^\s*\d+\.\s+/.test(current)) { const items = []; while (index < lines.length && /^\s*\d+\.\s+/.test(lines[index])) items.push(`<li>${inline(lines[index].replace(/^\s*\d+\.\s+/, ""))}</li>`), index += 1; output.push(`<ol>${items.join("")}</ol>`); continue; } if (/^\s*>/.test(current)) { const quote = []; while (index < lines.length && /^\s*>/.test(lines[index])) quote.push(inline(lines[index].replace(/^\s*>\s?/, ""))), index += 1; output.push(`<blockquote>${quote.join("<br />")}</blockquote>`); continue; } const paragraph = [current]; index += 1; while (index < lines.length && lines[index].trim() && !special(lines[index], index)) paragraph.push(lines[index++]); output.push(`<p>${inline(paragraph.join("\n")).replace(/\n/g, "<br />")}</p>`); } return output.join(""); }
  function localizeError(message) { const text = String(message || ""); if (/already active/i.test(text)) return t("error.runConflict"); if (/run not found/i.test(text)) return t("error.runNotFound"); if (/report not found/i.test(text)) return t("error.reportNotFound"); if (/unsupported characters/i.test(text)) return t("error.unsupportedTicker"); if (/after asset filtering/i.test(text)) return t("error.effectiveAnalysts"); if (/analysis worker failed/i.test(text)) return t("error.failed"); return text || t("error.generic"); }
  function api(path, options) { return fetch(path, { headers: { Accept: "application/json", ...(options && options.headers) }, ...options }).then(async (response) => { const body = await response.json().catch(() => ({})); if (!response.ok) { if (response.status === 422) throw new Error(t("error.validation")); throw new Error(localizeError(body.detail)); } return body; }).catch((error) => { if (error instanceof TypeError) throw new Error(t("error.unavailable")); throw error; }); }
  function formatElapsed(startedAt) { const seconds = Math.max(0, Math.floor((Date.now() - new Date(startedAt).getTime()) / 1000)); return t("run.elapsed", { time: `${String(Math.floor(seconds / 60)).padStart(2, "0")}:${String(seconds % 60).padStart(2, "0")}` }); }
  function startElapsed(startedAt) { state.startedAt = startedAt || new Date().toISOString(); clearInterval(state.elapsedTimer); $("elapsed-time").textContent = formatElapsed(state.startedAt); state.elapsedTimer = setInterval(() => { if (state.startedAt) $("elapsed-time").textContent = formatElapsed(state.startedAt); }, 1000); }
  function stopElapsed() { clearInterval(state.elapsedTimer); state.elapsedTimer = null; }
  function resetRunState() { stopElapsed(); state.lastSeq = 0; state.seen = new Set(); state.reportId = null; state.runRecord = null; state.startedAt = null; state.phases = PHASE_KEYS.map((key) => ({ key, status: "pending" })); $("activity-feed").innerHTML = ""; $("terminal-panel").hidden = true; $("report-panel").hidden = true; $("run-grid").hidden = false; $("progress-bar").style.width = "0%"; $("progress-label").textContent = "0%"; $("event-count").textContent = t("run.events", { count: 0 }); $("elapsed-time").textContent = t("run.elapsed", { time: "00:00" }); renderPhases(); }
  function switchView(view) { setupView.hidden = view !== "setup"; libraryView.hidden = view !== "library"; settingsView.hidden = view !== "settings"; runView.hidden = view !== "run"; document.querySelectorAll(".nav-button").forEach((button) => button.classList.toggle("is-active", button.dataset.view === view)); }
  function showSetup() { stopElapsed(); if (state.source) state.source.close(); state.source = null; state.runId = null; state.archived = false; switchView("setup"); setConnection("ready"); api("/api/history").then(renderHistory).catch(() => {}); loadWatchlist(); }
  function showLibrary() { stopElapsed(); if (state.source) state.source.close(); state.source = null; state.runId = null; switchView("library"); setConnection("ready"); renderLibrary(); api("/api/history").then(renderHistory).then(renderLibrary).catch(() => {}); }
  async function showSettings() { stopElapsed(); if (state.source) state.source.close(); state.source = null; state.runId = null; state.archived = false; switchView("settings"); setConnection("ready"); try { const [settings, providers] = await Promise.all([api("/api/settings"), api("/api/providers/market-data")]); const fields = settings.fields || {}; $("settings-fields").innerHTML = Object.entries(fields).map(([key, value]) => `<div><dt>${escapeHtml(SETTINGS_LABELS[key] || key)}</dt><dd>${escapeHtml(typeof value === "object" ? `${value.value ?? ""}（来源：${value.source ?? "未知"}）` : value)}</dd></div>`).join(""); $("provider-status-list").innerHTML = (providers.providers || []).map((item) => `<div class="provider-status"><strong>${escapeHtml(item.label || item.id)}</strong><span class="status-chip ${item.status}">${escapeHtml(item.status === "ready" ? "可用" : item.status === "not_configured" ? "未配置" : "异常")}</span></div>`).join("") || `<p class="muted">暂无行情来源。</p>`; } catch (_) { $("settings-fields").innerHTML = `<p class="muted">设置暂时不可用。</p>`; } }
  function renderRunHeader(record) { const request = record.request || {}; $("run-title").textContent = t("run.briefingTitle", { ticker: request.ticker || t("report.decisionReport") }); const asset = request.asset_type === "crypto" ? t("assets.crypto") : request.asset_type === "stock" ? t("assets.stock") : request.asset_type || ""; $("run-subtitle").textContent = [request.analysis_date, asset, request.research_depth ? `${t("form.researchDepth")} ${request.research_depth}` : ""].filter(Boolean).join(" · "); }
  function rememberActiveRun(record) { if (record?.run_id && ["queued", "running"].includes(record.status)) localStorage.setItem(ACTIVE_RUN_KEY, record.run_id); else if (!record || record.run_id === localStorage.getItem(ACTIVE_RUN_KEY)) localStorage.removeItem(ACTIVE_RUN_KEY); }
  function showRun(record) { state.runRecord = record; rememberActiveRun(record); switchView("run"); $("run-grid").hidden = ["completed", "interrupted", "failed", "cancelled"].includes(record.status); $("report-panel").hidden = record.status !== "completed"; $("run-header").hidden = record.status === "completed"; renderRunHeader(record); if (!state.archived && ["queued", "running"].includes(record.status)) startElapsed(record.started_at || new Date().toISOString()); }
  function renderPhases() { $("phase-timeline").innerHTML = state.phases.map((phase) => `<li class="is-${phase.status}"><span class="phase-dot"></span><div><span class="phase-name">${escapeHtml(t(`phase.${phase.key}`))}</span><span class="phase-status">${escapeHtml(t(`status.${phase.status}`))}</span></div></li>`).join(""); }
  function addActivity(title, summary, timestamp) { const feed = $("activity-feed"); const entry = document.createElement("article"); entry.className = "activity-entry"; entry.innerHTML = `<time>${escapeHtml(new Date(timestamp || Date.now()).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }))}</time><strong>${escapeHtml(translateDynamic(title))}</strong><p>${escapeHtml(summary)}</p>`; feed.appendChild(entry); feed.scrollTop = feed.scrollHeight; $("event-count").textContent = t("run.events", { count: feed.children.length }); }
  function updatePhase(name) { const key = PHASE_NAME_KEYS[name]?.replace("phase.", "") || name; const index = PHASE_KEYS.indexOf(key); if (index < 0) return; state.phases = state.phases.map((phase, i) => ({ key: phase.key, status: i < index ? "done" : i === index ? "in_progress" : "pending" })); renderPhases(); }
  function processEvent(envelope) { if (!envelope || envelope.run_id !== state.runId || state.seen.has(envelope.seq)) return; state.seen.add(envelope.seq); state.lastSeq = Math.max(state.lastSeq, Number(envelope.seq) || 0); const payload = envelope.payload || {}; switch (envelope.event) { case "run_started": setConnection("live"); startElapsed(state.runRecord?.started_at || envelope.timestamp); addActivity("run.deskStarted", t("run.analystsAssigned", { count: payload.analysts?.length || 0 }), envelope.timestamp); break; case "phase_changed": updatePhase(payload.phase); addActivity(payload.phase, payload.status === "in_progress" ? t("run.phaseStarted") : t(`status.${payload.status}`), envelope.timestamp); break; case "agent_status": if (payload.status === "in_progress") $("current-agent").textContent = translateDynamic(payload.agent); addActivity(payload.agent, t(`status.${payload.status}`), envelope.timestamp); break; case "progress": { const percent = Math.round((Number(payload.progress) || 0) * 100); $("progress-bar").style.width = `${percent}%`; $("progress-label").textContent = `${percent}%`; if (payload.current_agent) $("current-agent").textContent = translateDynamic(payload.current_agent); break; } case "message": addActivity(t("run.fieldNotes"), payload.text || "", envelope.timestamp); break; case "activity": addActivity(payload.name || t("run.graphUpdate"), payload.summary || "", envelope.timestamp); break; case "run_completed": completeRun(payload); break; case "run_failed": terminalRun("failed", localizeError(payload.error_message) || t("error.failed")); break; case "run_cancelled": terminalRun("cancelled", t("error.cancelled")); break; case "run_interrupted": terminalRun("interrupted", t("error.interrupted")); break; case "run_snapshot": if (payload.run) syncRecord(payload.run); break; default: break; } }
  function syncRecord(record) { state.runRecord = record; rememberActiveRun(record); if (record.started_at && !state.archived && ["queued", "running"].includes(record.status)) startElapsed(record.started_at); if (record.status === "completed" && record.report_id) loadReport(record.report_id); else if (["failed", "cancelled", "interrupted"].includes(record.status)) terminalRun(record.status, record.error_message || t(`error.${record.status}`)); }
  function reconnectStatus() { if (!state.runId) return Promise.resolve(); return api(`/api/runs/${encodeURIComponent(state.runId)}`).then((record) => { syncRecord(record); return record; }).catch(() => null); }
  async function restoreActiveRun() { let record = null; const savedId = localStorage.getItem(ACTIVE_RUN_KEY); if (savedId) { try { record = await api(`/api/runs/${encodeURIComponent(savedId)}`); } catch (_) { localStorage.removeItem(ACTIVE_RUN_KEY); } } if (!record) { try { record = (await api("/api/runs/active")).run; } catch (_) {} } if (!record) return; if (!["queued", "running"].includes(record.status)) { rememberActiveRun(record); return; } resetRunState(); state.runId = record.run_id; state.archived = false; showRun(record); connectEvents(); }
  function connectEvents() { if (!state.runId) return; if (state.source) state.source.close(); const url = `/api/runs/${encodeURIComponent(state.runId)}/events?after_seq=${state.lastSeq}`; const source = new EventSource(url); state.source = source; source.onopen = () => setConnection("live"); source.onmessage = (event) => { try { processEvent(JSON.parse(event.data)); } catch (_) {} }; ["run_snapshot", "run_started", "phase_changed", "agent_status", "progress", "message", "activity", "run_completed", "run_failed", "run_cancelled", "run_interrupted"].forEach((name) => source.addEventListener(name, (event) => { try { processEvent(JSON.parse(event.data)); } catch (_) {} })); source.onerror = () => { setConnection("reconnecting"); reconnectStatus().then((record) => { if (record && ["completed", "failed", "cancelled", "interrupted"].includes(record.status)) { source.close(); state.source = null; return; } if (source.readyState === EventSource.CLOSED) { source.close(); setTimeout(connectEvents, 1000); } }); }; }
  function completeRun(payload) { rememberActiveRun({ run_id: state.runId, status: "completed" }); stopElapsed(); state.reportId = payload.report_id; $("cancel-run").hidden = true; $("new-analysis").hidden = false; $("progress-bar").style.width = "100%"; $("progress-label").textContent = "100%"; state.phases = state.phases.map((phase) => ({ ...phase, status: "done" })); renderPhases(); setConnection("complete"); addActivity(t("run.briefingReady"), payload.signal ? t("run.signal", { signal: payload.signal }) : t("run.reportGenerated"), Date.now()); loadReport(payload.report_id); api("/api/history").then(renderHistory).catch(() => {}); }
  function terminalRun(status, message) { rememberActiveRun({ run_id: state.runId, status }); stopElapsed(); $("cancel-run").hidden = true; $("new-analysis").hidden = false; $("run-grid").hidden = true; $("run-header").hidden = false; $("terminal-panel").hidden = false; $("terminal-panel").innerHTML = `<h3>${escapeHtml(status === "failed" ? t("error.failed") : status === "interrupted" ? t("error.interrupted") : t("connection.cancelled"))}</h3><p>${escapeHtml(message)}</p>`; setConnection(status); }
  async function loadReport(reportId) { try { const report = await api(`/api/history/${encodeURIComponent(reportId)}`); $("run-grid").hidden = true; $("run-header").hidden = true; renderReport(report); } catch (error) { terminalRun("failed", error.message); } }
  function renderReport(report) { $("run-grid").hidden = true; $("run-header").hidden = true; $("report-panel").hidden = false; $("report-title").textContent = t("report.title", { ticker: report.ticker || t("report.decisionReport") }); const request = state.runRecord?.request || {}; const metadata = [["report.signal", report.rating || report.signal], ["report.date", report.analysis_date || request.analysis_date], ["report.asset", report.asset_type === "crypto" ? t("assets.crypto") : report.asset_type === "stock" ? t("assets.stock") : report.asset_type], ["report.depth", report.research_depth || request.research_depth], ["report.provider", report.provider || request.provider], ["report.quickModel", report.quick_model || request.quick_model], ["report.deepModel", report.deep_model || request.deep_model], ["report.language", languageLabel(report.output_language || request.output_language)], ["report.source", report.source === "legacy" ? t("library.legacy") : report.source], ["report.dataStatus", report.data_status || "unknown"], ["report.snapshot", report.data_snapshot_id || "未生成"]].filter(([, value]) => value !== null && value !== undefined && value !== ""); $("report-meta").innerHTML = metadata.map(([label, value]) => `<div><dt>${escapeHtml(t(label))}</dt><dd>${escapeHtml(value)}</dd></div>`).join(""); $("download-report").onclick = () => { window.location.href = `/api/history/${encodeURIComponent(report.report_id)}/download`; }; const summary = report.executive_summary || ""; $("executive-summary").hidden = !summary; $("executive-summary-content").innerHTML = summary ? renderMarkdown(summary) : ""; $("detail-report").open = !summary; const sections = REPORT_GROUPS.map(([title, group, sectionList]) => { const blocks = sectionList.map(([label, key]) => report[group]?.[key] ? `<article class="report-section"><h3>${escapeHtml(t(label))}</h3>${renderMarkdown(report[group][key])}</article>` : "").join(""); return blocks ? `<section><h2>${escapeHtml(t(title))}</h2>${blocks}</section>` : ""; }).join(""); $("report-content").innerHTML = sections || renderMarkdown(report.complete_report || t("library.metadataUnavailable")); }
  function historyItem(record, library) { const status = record.rating || record.signal || (record.status ? t(`status.${record.status}`) : t("status.report")); const freshness = record.data_status ? `数据：${record.data_status}` : ""; const metadata = [record.analysis_date, record.asset_type ? (record.asset_type === "crypto" ? t("assets.crypto") : t("assets.stock")) : "", record.provider || "", freshness].filter(Boolean).join(" · "); return `<article class="${library ? "library-item" : "history-item"}"><button type="button" data-report-id="${escapeHtml(record.report_id)}"><span class="history-ticker">${escapeHtml(record.ticker || t("history.unknown"))}</span><span class="history-meta">${escapeHtml(metadata)}</span></button><span class="history-signal">${escapeHtml(status)}</span><span class="history-preview">${escapeHtml(record.decision_preview || "")}</span><button class="text-button" type="button" data-report-id="${escapeHtml(record.report_id)}">${escapeHtml(t("actions.open"))}</button></article>`; }
  function bindReportLinks(container) { container.querySelectorAll("[data-report-id]").forEach((button) => button.addEventListener("click", () => reopenHistory(button.dataset.reportId))); }
  function renderHistory(records) { state.history = records || []; const list = $("history-list"); if (!state.history.length) { list.innerHTML = `<p class="muted">${escapeHtml(t("history.empty"))}</p>`; return; } list.innerHTML = state.history.slice(0, 5).map((record) => historyItem(record, false)).join(""); bindReportLinks(list); }
  function renderWatchlist(items, quotes = {}) { state.watchlist.items = items || []; const list = $("watchlist-list"); if (!state.watchlist.items.length) { list.innerHTML = `<p class="muted">关注列表为空，请添加一个资产。</p>`; return; } list.innerHTML = state.watchlist.items.map((item) => { const quote = quotes[item.symbol] || {}; const value = quote.price == null ? "暂无行情" : `${escapeHtml(Number(quote.price).toLocaleString(undefined, { maximumFractionDigits: 6 }))} ${escapeHtml(quote.currency || "")}`; const change = quote.change_percent == null ? "" : `<span class="${Number(quote.change_percent) >= 0 ? "quote-up" : "quote-down"}">${Number(quote.change_percent) >= 0 ? "+" : ""}${escapeHtml(Number(quote.change_percent).toFixed(2))}%</span>`; const source = quote.source ? `来源：${escapeHtml(quote.source)}` : escapeHtml(quote.error?.message || "数据源暂不可用"); return `<article class="watchlist-card"><div class="watchlist-card-head"><div><span class="symbol">${escapeHtml(item.symbol)}</span><span class="asset-type">${item.asset_type === "crypto" ? "加密货币" : "股票"}</span></div><button type="button" class="icon-button" data-remove-watchlist="${escapeHtml(item.id)}" data-version="${escapeHtml(state.watchlist.version)}" aria-label="移除 ${escapeHtml(item.symbol)}">×</button></div><div class="quote-value">${value} ${change}</div><div class="quote-meta">${source}</div><button type="button" class="text-button watchlist-analyze" data-analyze-symbol="${escapeHtml(item.symbol)}" data-analyze-asset="${escapeHtml(item.asset_type)}">开始分析</button></article>`; }).join(""); list.querySelectorAll("[data-analyze-symbol]").forEach((button) => button.addEventListener("click", () => { $("ticker").value = button.dataset.analyzeSymbol; $("asset-type").value = button.dataset.analyzeAsset; toggleCryptoAnalyst(); showSetup(); $("analysis-form").scrollIntoView({ behavior: "smooth", block: "start" }); })); list.querySelectorAll("[data-remove-watchlist]").forEach((button) => button.addEventListener("click", async () => { try { await api(`/api/watchlist/items/${encodeURIComponent(button.dataset.removeWatchlist)}?version=${encodeURIComponent(button.dataset.version)}`, { method: "DELETE" }); await loadWatchlist(); } catch (error) { $("watchlist-error").textContent = localizeError(error.message); } })); }
  async function loadWatchlist() { try { $("watchlist-error").textContent = ""; const response = await api("/api/watchlist"); state.watchlist.version = response.watchlist?.version || 1; const items = response.items || []; if (!items.length) { renderWatchlist(items); return; } const grouped = items.reduce((result, item) => { const key = item.asset_type || "stock"; (result[key] ||= []).push(item.symbol); return result; }, {}); const quotes = {}; await Promise.all(Object.entries(grouped).map(async ([assetType, symbols]) => { try { const response = await api(`/api/quotes?symbols=${encodeURIComponent(symbols.join(","))}&asset_type=${encodeURIComponent(assetType)}`); (response.items || []).forEach((item) => { quotes[item.symbol] = item; }); } catch (_) {} })); renderWatchlist(items, quotes); } catch (error) { $("watchlist-list").innerHTML = `<p class="muted">关注列表暂时不可用。</p>`; $("watchlist-error").textContent = localizeError(error.message); } }
  async function addWatchlistItem(event) { event.preventDefault(); const symbol = $("watchlist-symbol").value.trim(); if (!symbol) { $("watchlist-error").textContent = "请输入要关注的标的代码。"; return; } try { await api("/api/watchlist/items", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ symbol, asset_type: $("watchlist-asset-type").value }) }); $("watchlist-symbol").value = ""; await loadWatchlist(); } catch (error) { $("watchlist-error").textContent = localizeError(error.message); } }
  function renderLibrary() { const list = $("library-list"); const query = state.filters.search.trim().toLowerCase(); let records = state.history.filter((record) => (!state.filters.asset || record.asset_type === state.filters.asset) && (!state.filters.status || record.status === state.filters.status) && (!query || `${record.ticker || ""} ${record.decision_preview || ""}`.toLowerCase().includes(query))); records = records.filter((record) => record.status === "completed" || record.source === "legacy" || !record.status); records.sort((a, b) => { const left = new Date(a.generated_at || a.analysis_date || 0).getTime(); const right = new Date(b.generated_at || b.analysis_date || 0).getTime(); return state.filters.sort === "oldest" ? left - right : right - left; }); if (!records.length) { list.innerHTML = `<p class="muted">${escapeHtml(state.history.length ? t("library.noMatches") : t("history.empty"))}</p>`; return; } list.innerHTML = records.map((record) => historyItem(record, true)).join(""); bindReportLinks(list); }
  async function reopenHistory(reportId) { const record = state.history.find((item) => item.report_id === reportId) || {}; resetRunState(); state.archived = true; showRun({ status: "completed", run_id: record.run_id, request: { ticker: record.ticker || t("history.title"), analysis_date: record.analysis_date || "", asset_type: record.asset_type || "", research_depth: record.research_depth || "", provider: record.provider, quick_model: record.quick_model, deep_model: record.deep_model, output_language: record.output_language } }); $("cancel-run").hidden = true; await loadReport(reportId); }
  function renderModelOptions(config) { const providers = config.providers || []; const providerSelect = $("provider"); const current = providerSelect.value || config.configured?.provider || config.provider; providerSelect.innerHTML = providers.map((item) => `<option value="${escapeHtml(item.value)}">${escapeHtml(item.label)}</option>`).join(""); providerSelect.value = providers.some((item) => item.value === current) ? current : providers[0]?.value || ""; const selected = providers.find((item) => item.value === providerSelect.value) || providers[0]; const selectedQuick = $("quick-model").value || config.configured?.quick_model; const selectedDeep = $("deep-model").value || config.configured?.deep_model; $("quick-model").innerHTML = (selected?.quick_models || []).map((item) => `<option value="${escapeHtml(item.value)}">${escapeHtml(item.label)}</option>`).join(""); $("deep-model").innerHTML = (selected?.deep_models || []).map((item) => `<option value="${escapeHtml(item.value)}">${escapeHtml(item.label)}</option>`).join(""); $("quick-model").value = (selected?.quick_models || []).some((item) => item.value === selectedQuick) ? selectedQuick : selected?.quick_models?.[0]?.value || ""; $("deep-model").value = (selected?.deep_models || []).some((item) => item.value === selectedDeep) ? selectedDeep : selected?.deep_models?.[0]?.value || ""; $("provider-summary").textContent = `${selected?.label || t("provider.local")} · ${$("deep-model").value || t("provider.configured")}`; }
  function renderLanguages(config) { const select = $("output-language"); const current = select.value || config.configured?.output_language || config.output_language || "English"; select.innerHTML = (config.output_languages || []).map((item) => `<option value="${escapeHtml(item.value)}">${escapeHtml(languageLabel(item.value))}</option>`).join(""); select.value = (config.output_languages || []).some((item) => item.value === current) ? current : "English"; }
  function renderFormOptions(config) { const selectedAnalysts = new Set([...form.querySelectorAll('input[name="analysts"]:checked')].map((input) => input.value)); const analystDefaults = selectedAnalysts.size ? selectedAnalysts : new Set((config.analyst_options || []).slice(0, 2).map((item) => item.key)); const selectedDepth = form.querySelector('input[name="research_depth"]:checked')?.value; $("analyst-options").innerHTML = (config.analyst_options || []).map((item) => `<div class="choice"><input id="analyst-${escapeHtml(item.key)}" type="checkbox" name="analysts" value="${escapeHtml(item.key)}" ${analystDefaults.has(item.key) ? "checked" : ""} /><label for="analyst-${escapeHtml(item.key)}">${escapeHtml(t(`analysts.${item.key}`))}</label></div>`).join(""); $("depth-options").innerHTML = (config.research_depths || [1, 3, 5]).map((depth, index) => `<div class="choice"><input id="depth-${depth}" type="radio" name="research_depth" value="${depth}" ${String(depth) === String(selectedDepth ?? (config.research_depths || [1])[0]) || (!selectedDepth && index === 0) ? "checked" : ""} /><label for="depth-${depth}">${escapeHtml(t(depth === 1 ? "depth.quick" : depth === 3 ? "depth.balanced" : "depth.deep"))}</label></div>`).join(""); renderModelOptions(config); renderLanguages(config); }
  function setupForm(config) { state.config = config; $("analysis-date").value = config.default_date || new Date().toISOString().slice(0, 10); renderFormOptions(config); $("asset-type").addEventListener("change", toggleCryptoAnalyst); $("provider").addEventListener("change", () => renderModelOptions(state.config)); toggleCryptoAnalyst(); }
  function toggleCryptoAnalyst() { const disabled = $("asset-type").value === "crypto"; const fundamentals = $("analyst-fundamentals"); if (!fundamentals) return; fundamentals.checked = false; fundamentals.disabled = disabled; fundamentals.closest(".choice").classList.toggle("is-disabled", disabled); }
  async function submitRun(event) { event.preventDefault(); $("form-error").textContent = ""; const analysts = [...form.querySelectorAll('input[name="analysts"]:checked')].map((input) => input.value); if (!$("ticker").value.trim()) { $("form-error").textContent = t("error.ticker"); return; } if (!analysts.length) { $("form-error").textContent = t("error.analysts"); return; } const body = { ticker: $("ticker").value.trim(), analysis_date: $("analysis-date").value, asset_type: $("asset-type").value, analysts, research_depth: Number(form.querySelector('input[name="research_depth"]:checked')?.value || 1), provider: $("provider").value, quick_model: $("quick-model").value, deep_model: $("deep-model").value, output_language: $("output-language").value, quote_strategy_id: state.config?.effective_quote_strategy_id || "default-yfinance" }; try { const record = await api("/api/runs", { method: "POST", body: JSON.stringify(body), headers: { "Content-Type": "application/json" } }); resetRunState(); state.runId = record.run_id; state.archived = false; showRun(record); $("cancel-run").hidden = false; $("new-analysis").hidden = true; connectEvents(); } catch (error) { $("form-error").textContent = localizeError(error.message); } }
 $("cancel-run").addEventListener("click", async () => { if (!state.runId) return; try { await api(`/api/runs/${encodeURIComponent(state.runId)}/cancel`, { method: "POST" }); } catch (error) { terminalRun("failed", error.message); } }); $("back-history").addEventListener("click", () => state.archived ? showLibrary() : showSetup()); $("new-analysis").addEventListener("click", showSetup); $("open-library").addEventListener("click", showLibrary); $("library-new-analysis").addEventListener("click", showSetup); $("refresh-history").addEventListener("click", () => api("/api/history").then(renderHistory).catch(() => {})); $("library-search").addEventListener("input", (event) => { state.filters.search = event.target.value; renderLibrary(); }); $("library-asset-filter").addEventListener("change", (event) => { state.filters.asset = event.target.value; renderLibrary(); }); $("library-status-filter").addEventListener("change", (event) => { state.filters.status = event.target.value; renderLibrary(); }); $("library-sort").addEventListener("change", (event) => { state.filters.sort = event.target.value; renderLibrary(); }); form.addEventListener("submit", submitRun);
  $("refresh-quotes").addEventListener("click", loadWatchlist);
  $("watchlist-form").addEventListener("submit", addWatchlistItem);
  document.querySelectorAll(".nav-button").forEach((button) => button.addEventListener("click", () => {
    const view = button.dataset.view;
    if (view === "setup") showSetup();
    else if (view === "library") showLibrary();
    else if (view === "settings") showSettings();
  }));
  state.language = "zh"; applyTranslations(); renderPhases(); api("/api/config").then(setupForm).catch(() => { $("provider-summary").textContent = "本地配置不可用"; }); api("/api/history").then(renderHistory).catch(() => { $("history-list").innerHTML = `<p class="muted">历史记录不可用。</p>`; }); loadWatchlist(); restoreActiveRun();
})();
